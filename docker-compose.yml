services:
  app:
    build: .
    container_name: demo-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    # For Dokploy: remove env_file and set environment variables in the Dokploy UI,
    # or use Dokploy's built-in env management.
    # environment:
    #   - PORT=3000
    #   - SMTP_HOST=smtp.resend.com
    #   - SMTP_PORT=587
    #   - SMTP_USER=resend
    #   - SMTP_PASS=re_xxxxxxxx
    #   - SMTP_FROM=you@yourdomain.com
    #   - SMTP_TO=you@yourdomain.com
    #   - DEEPSEEK_API_KEY=sk-...
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
